apiVersion: apigee.cloud.google.com/v1alpha2
kind: ApigeeEnvironment
metadata:
  name: env
spec:
  components:
    synchronizer:
      podServiceAccountName: "apigee-synchronizer-svc-account-${ORGANIZATION_NAME}-${ENVIRONMENT_NAME}" # kpt-set: apigee-synchronizer-svc-account-${ORGANIZATION_NAME}-${ENVIRONMENT_NAME}
    udca:
      podServiceAccountName: "apigee-udca-svc-account-${ORGANIZATION_NAME}-${ENVIRONMENT_NAME}" # kpt-set: apigee-udca-svc-account-${ORGANIZATION_NAME}-${ENVIRONMENT_NAME}
    runtime:
      podServiceAccountName: "apigee-runtime-svc-account-${ORGANIZATION_NAME}-${ENVIRONMENT_NAME}" # kpt-set: apigee-runtime-svc-account-${ORGANIZATION_NAME}-${ENVIRONMENT_NAME}